#include<bits/stdc++.h>
#define rep(i,a,b) for(int i=a;i<b;i++)
#define ll long long int
#define vi vector<ll>

using namespace std;
int main()
{
    ll n,m;
    cin>>n>>m;
    via(m);
    rep(i,0,m)cin>>a[i];
    multiset<ll> s;
    rep(i,0,n)s.insert(0);
    vi ans;
    rep(i,0,m)
    {
        li t=
    }
}